package main

import(
    "fmt"
    "math"
)

const s = "HELLO WORLD"
func main() {
    if 1 == 1 {
        fmt.Println("TRUE")
    }
    for b, j := 5, 3.3; j < float64(b); j += float64(1) {
        fmt.Println(b, j, s)
    }
    INADDR_ANY  INADDR_ANY
    const a int32 = 3
    fmt.Println(s, a)
    fmt.Println(math.Sin_float32(float32(a)))
    var checktype = func(i interface{}) {
        switch i.(type) {
        case int32:
            fmt.Println("INT")
        default:
            fmt.Println("DEFAULT")
        }
    }
    checktype(checktype)
}
